<tr>
  <td><%= admin_purchase.user.email %></td>
  <td><%= admin_purchase.product.name %></td>
  <td><%= admin_purchase.quantity %></td>
  <td><%= admin_purchase.created_at.getlocal.strftime("%e-%m-%y %H:%M") %></td>
</tr>
